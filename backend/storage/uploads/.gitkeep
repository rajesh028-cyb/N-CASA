This directory holds uploaded configuration files during development.

Each audit has its own subdirectory:

  storage/uploads/<AUDIT_ID>/<filename>

Example:
  storage/uploads/AUD-8F31C2/router.cfg

Block 11: this local storage will be replaced by database-managed
          storage (PostgreSQL + cloud object store).

Do NOT commit real configuration files to version control.
